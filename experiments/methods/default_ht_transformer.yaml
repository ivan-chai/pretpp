defaults:
  - default_transformer
  - _self_

module:
  seq_encoder:
    model_partial:
      _target_: pretpp.nn.HistoryTokenTransformer
      strategy_partial:
        _target_: pretpp.nn.FullHTStrategy
        _partial_: true
        apply_probability: ${transformer_history_token_probability}
  aggregator: null
