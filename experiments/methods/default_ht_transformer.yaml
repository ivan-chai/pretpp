defaults:
  - default_transformer
  - _self_

module:
  seq_encoder:
    model_partial:
      _target_: pretpp.nn.HistoryTokenTransformer
      history_token_locality: ${transformer_history_token_locality}
      history_token_fraction: ${transformer_history_token_fraction}
  aggregator: null
