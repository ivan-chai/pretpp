defaults:
  - default
  - default_rnn
  - default_supervised
  - _self_

name: supervised_rnn_metric

module:
  head_partial:
    _target_: pretpp.nn.NormalizationHead
    _partial_: true
  loss_projection_partial: ${metric_head}
