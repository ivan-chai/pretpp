defaults:
  - default
  - default_transformer
  - mtl_loss
  - _self_

name: ntp_cls_transformer_last

apply_cls_to_tokens: last
cls_token: ${transformer_cls_token}

module:
  _target_: pretpp.modules.BaseModule
  loss:
    truncate: begin
