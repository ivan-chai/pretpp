defaults:
  - default_transformer
  - _self_

module:
  seq_encoder:
    model_partial:
      _target_: pretpp.nn.RecurrentMemoryTransformer
      chunk_size: ${transformer_recmem_chunk_size}
      n_tokens: 3
      multitoken: true
  aggregator: null
