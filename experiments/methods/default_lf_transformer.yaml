defaults:
  - default_transformer
  - _self_

module:
  seq_encoder:
    model_partial:
      _target_: pretpp.nn.LongFormer
      kernel_size: null
      global_frequency: 0.1
      causal: false
  aggregator: null
