defaults:
  - default
  - default_hts_transformer_rope
  - next_item_loss
  - _self_

name: next_item_hts_transformer_rope

module:
  _target_: pretpp.modules.BaseModule
  loss_projection_partial: ${head}
