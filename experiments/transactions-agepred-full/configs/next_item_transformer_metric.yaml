defaults:
  - next_item_transformer
  - _self_

name: next_item_transformer_metric

module:
  head_partial:
    _target_: pretpp.nn.NormalizationHead
    _partial_: true
  loss_projection_partial: ${metric_head}
