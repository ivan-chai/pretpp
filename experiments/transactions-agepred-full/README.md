A special AgePred split with the following parts:
- a standard test set with 10% of elements (all elements are labeled)
- validation includes only labeled elements (~5% of the development set, all elements are labeled)
- training set includes all unlabeled data and the remaining labeled elements
