defaults:
  - default
  - default_rm_transformer
  - next_item_loss
  - _self_

name: next_item_rm_transformer

data_module:
  batch_size: 256

module:
  _target_: pretpp.modules.BaseModule
  loss_projection_partial: ${head}
