defaults:
  - default
  - default_rnn
  - default_ft
  - _self_

base_name: coles

module:
  _target_: pretpp.modules.BaseModule
  head_partial:
    _target_: pretpp.nn.NormalizationHead
    _partial_: true
  loss_projection_partial: ${head}
