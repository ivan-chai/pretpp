defaults:
  - default
  - default_birnn
  - mlm_loss@module.loss
  - _self_

name: mlm

module:
  _target_: pretpp.modules.BaseModule
  seq_encoder:
    embedder:
      embeddings:
        labels:
          in: ${num_classes_ext}
  loss_projection_partial: ${head}
