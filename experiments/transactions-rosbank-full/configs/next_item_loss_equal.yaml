defaults:
  - next_item_loss
  - _self_

module:
  loss:
    losses:
      timestamps:
        grad_scale: null
      labels:
        grad_scale: null
      currency:
        grad_scale: null
      channel_type:
        grad_scale: null
      trx_category:
        grad_scale: null
      log_amount:
        grad_scale: null
