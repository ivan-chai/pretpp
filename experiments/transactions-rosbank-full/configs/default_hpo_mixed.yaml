defaults:
  - default_hpo
  - _self_

module:
  shared_prefix: _loss_projection.
  hpo_losses:
    - loss_0_timestamps
    - loss_0_labels
    - loss_0_currency
    - loss_0_channel_type
    - loss_0_trx_category
    - loss_0_log_amount
    - loss_1_target_flag
