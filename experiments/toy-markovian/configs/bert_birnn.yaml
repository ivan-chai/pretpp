defaults:
  - default
  - default_birnn
  - bert_loss
  - _self_

name: bert_birnn

module:
  _target_: pretpp.modules.BaseModule
  loss_projection_partial: ${head}
  loss:
    coles_loss:
      embedding_dim: ${rnn_hidden_size}
