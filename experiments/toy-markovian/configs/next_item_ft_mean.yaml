defaults:
  - default
  - default_rnn
  - default_ft
  - _self_

base_name: next_item
name: ${base_name}_ft_mean

module:
  _target_: pretpp.modules.BaseModule
  loss_projection_partial: ${head}
  aggregator:
    _target_: hotpp.nn.MeanAggregator
